{{ form_start(form) }}
    {{ form_widget(form) }}
<button class="btn btn-secondary mb-3 offset-lg-11 offset-sm-10">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
        {% block stylesheets %}
            {{ encore_entry_link_tags('article') }}
        {% endblock %}
