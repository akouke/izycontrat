{% extends '_layout.html.twig' %}

{% block title %}Création Newsletter{% endblock %}

{% block body %}
    <section class="top-content">
        <h1>Creation Newsletter</h1>

        {{ include('newsletter/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('newsletter_index') }}">back to list</a>

        {{ include('newsletter/_delete_form.html.twig') }}
    </section>

{% endblock %}
